{
  "name": "allenz-portfolio-visual",
  "private": true,
  "description": "Local visual-regression baselines for allenz.net (Playwright)",
  "scripts": {
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots",
    "test:visual:report": "playwright show-report",
    "test:visual:docker": "bash tests/run-in-docker.sh",
    "test:visual:docker:update": "bash tests/run-in-docker.sh update"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
